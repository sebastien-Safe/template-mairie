{
  "name": "template-mairie",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler deploy",
    "deploy:oauth-worker": "wrangler deploy --config oauth-worker/wrangler.jsonc"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "wrangler": "^4.124.0"
  }
}
